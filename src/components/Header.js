import React, {Component} from "react";

import "../styles/Header.css"

class Header extends Component {
    render () {
        return(
            <header>Моя погода</header>
        )
    }
}

export default Header